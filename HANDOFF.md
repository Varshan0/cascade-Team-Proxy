# Backend handoff

This visual prototype has no live API calls yet. The next integration slice should replace mock presentation values in `src/main.tsx` with a typed API layer for price, liquidation feed, radar state, provider health, and paper account data. The terminal preview is a responsive UI scaffold; no order mutation is currently sent.

